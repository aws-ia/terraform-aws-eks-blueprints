apiVersion: v1
kind: Pod
metadata:
  name: fsx-taxi-exec
  namespace: emr-data-team-a
spec:
  volumes:
    - name: spark-local-dir-ny-taxi
      persistentVolumeClaim:
        claimName: fsx-super-dynamic-pvc

  nodeSelector:
    NodeGroupType: spark
#    topology.kubernetes.io/zone: us-west-2a
#    beta.kubernetes.io/instance-type: r5d.large

  containers:
    - name: spark-kubernetes-executor # Don't change this name. EMR on EKS looking for this name
      volumeMounts:
        - name: spark-local-dir-ny-taxi
          mountPath: /dynamic
          readOnly: false
