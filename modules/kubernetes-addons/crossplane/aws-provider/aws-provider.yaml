---
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: ${aws-provider-name}
spec:
  package: xpkg.upbound.io/upbound/provider-aws:${coalesce(provider-aws-version, "v0.19.0")}
  controllerConfigRef:
    name: aws-controller-config
